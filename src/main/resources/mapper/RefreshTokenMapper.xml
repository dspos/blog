<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yetao.blog.mapper.RefreshTokenMapper">

    <insert id="saveToken">
        insert into tb_token(id, user_id, token_key, refresh_token, create_time, update_time)
        value(#{id}, #{userId}, #{tokenKey}, #{refreshToken}, #{createTime}, #{updateTime})
    </insert>
</mapper>